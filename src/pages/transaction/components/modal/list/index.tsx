import React from 'react';
import TaskBar from './task bar ';
import Table from './table';

const List = () => {
  return (
    <div>
      <TaskBar />
      <Table />
    </div>
  );
};

export default List;
